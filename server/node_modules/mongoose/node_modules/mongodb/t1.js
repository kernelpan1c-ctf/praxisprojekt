var MongoClient = require('./').MongoClient;

MongoClient.connect('mongodb://localhost:27017/page-testing', function (err, db) {
  collection = db.collection('test');

  collection.insertMany([{a:1}, {a:2}], {w:1}, function (err, docs) {
    if (err) {
      console.log("ERROR");
    }

    collection.find().sort({'a': -1}).toArray(function(err, items) {
      if (err) {
        console.log("ERROR");
      }
      console.log("Items: ", items);
    });
  });
});
// var database = null;
//
// var MongoClient = require('./').MongoClient;
//
// function connect_to_mongo(callback) {
//   if (database != null) {
//     callback(null, database);
//   } else {
//     var connection = "mongodb://127.0.0.1:27017/test_db";
//     MongoClient.connect(connection, {
//       server : {
//         reconnectTries : 5,
//         reconnectInterval: 1000,
//         autoReconnect : true
//       }
//     }, function (err, studdb) {
//       database = studdb;
//       callback(err, studdb);
//     });
//   }
// }
//
// function log(message) {
//   console.log(new Date(), message);
// }
//
// var queryNumber = 0;
//
// function make_query(studdb) {
//   var currentNumber = queryNumber;
//   ++queryNumber;
//   log("query " + currentNumber + ": started");
//
//   setTimeout(function() {
//       make_query(studdb);
//   }, 5000);
//
//   var collection = studdb.collection('test_collection');
//   collection.findOne({},
//     function (err, result) {
//       if (err != null) {
//         log("query " + currentNumber + ": find one error: " + err.message);
//         return;
//       }
//       log("query " + currentNumber + ": find one result: " + result);
//     }
//   );
// }
//
// connect_to_mongo(
//   function(err, studdb) {
//     if (err != null) {
//       log(err.message);
//       return;
//     }
//
//     make_query(studdb);
//   }
// );
